package constant

// Default constants (for global setting like static template, app name, etc.)

const StaticUrl = "/static"
const StaticPathPrefix = "web/static"
const AppName = "fiber-go-pos-app"
const SchemeDirectory = "./utils/schemes/"

// Base constants

const BaseURL = "http://localhost:9000/"
const BaseSISURL = BaseURL + "sis/"
const BasePOSURL = BaseURL + "pos/"

const CookiesKeyToken = "aplikasitokoonlineposjuditsatria"
